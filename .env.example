# Root .env — docker compose port overrides only.
# App secrets go in backend/.env
#
# Uncomment and change any port that conflicts with something already running locally.

# POSTGRES_PORT=5432
# BACKEND_PORT=8002
# FRONTEND_PORT=5174     # dev only
# FRONTEND_PROD_PORT=80  # prod only
